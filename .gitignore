.DS_Store
node_modules
.turbo
*.log
.next
dist
dist-ssr
*.local
.claude/settings.local.json
.env
.cache
server/dist
public/dist
storybook-static/
.source/
.open-next/

# Built skill packages (generated by scripts/build-skills.sh)
apps/docs/public/skills/*.tar.gz

# TypeScript incremental build artifacts
*.tsbuildinfo

# Playwright
test-results/
playwright-report/

# Local agent tooling, installed by the vercel-labs/skills installer.
# `.claude/skills/find-skills` is a symlink into `.agents/`, so tracking it
# without `.agents/` leaves a link that resolves to nothing on a fresh clone.
.agents/
.claude/skills/find-skills
.codex/
skills-lock.json
