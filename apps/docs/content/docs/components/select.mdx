---
title: Select
description: A dropdown control for selecting one or more options from a list.
---

## Import

```tsx
import {
  Select,
  SelectTrigger,
  SelectValue,
  SelectContent,
  SelectItem,
  SelectGroup,
  SelectLabel,
  SelectSeparator,
} from "@keystoneui/react/select";
```

## Usage

<ComponentPreview name="select-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/select.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="select-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Composition

```text
Select
├── SelectTrigger
│   └── SelectValue
└── SelectContent
    └── SelectGroup
        ├── SelectLabel
        ├── SelectItem
        └── SelectSeparator
```

## Examples

### Displaying labels in the trigger

`SelectValue` renders the selected option's `value` by default (e.g. `"us"`).
To show the label (e.g. `"United States"`) instead, pass an `items` map to
`Select` so it can resolve value → label:

```tsx
const countries = { us: "United States", ca: "Canada", uk: "United Kingdom" };

<Select items={countries} defaultValue="us">
  <SelectTrigger>
    <SelectValue />
  </SelectTrigger>
  <SelectContent>
    {Object.entries(countries).map(([value, label]) => (
      <SelectItem key={value} value={value}>{label}</SelectItem>
    ))}
  </SelectContent>
</Select>
```

For dynamic or computed display, pass a render function as `SelectValue`'s
child. This is the pattern used by `<ComponentPreview name="select-multiple" />`:

```tsx
<SelectValue>{(value) => labels[value] ?? "Select…"}</SelectValue>
```


### Groups

Organize options into labeled groups:

<ComponentPreview name="select-groups" />


### Scrollable

For long lists, the content scrolls automatically:

<ComponentPreview name="select-scrollable" />


### With Icons

Add icons to select items:

<ComponentPreview name="select-with-icons" />


### Multiple Selection

Select multiple values:

<ComponentPreview name="select-multiple" />


### Disabled

<ComponentPreview name="select-disabled" />


### Sizes

Select in different sizes:

<ComponentPreview name="select-sizes" />


### Controlled

Controlled select with state management:

<ComponentPreview name="select-controlled" />


### Form Example

Select used in a form context:

<ComponentPreview name="select-form-example" />

## RTL

This component supports RTL layouts via the standard `dir="rtl"` attribute on a parent element. See [the RTL guide](/docs/rtl).


{/* backlinks:start */}

## Related Blocks

- [Betting Panel 01](/docs/blocks/betting-panel-01) — A betting panel with inline ½ / 2× multipliers inside the bet amount field and risk-level toggles.
- [Betting Panel 03](/docs/blocks/betting-panel-03) — A betting panel with quick-pick preset amounts for rapid bet placement.
- [Tickets 01](/docs/blocks/tickets-01) — A sortable, filterable tickets table with inline cell editing, a new-ticket modal, and a floating bulk-edit action bar.

{/* backlinks:end */}

## API Reference

### Select

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `string` | - | Controlled selected value |
| `defaultValue` | `string` | - | Default selected value (uncontrolled) |
| `onValueChange` | `(value: string) => void` | - | Callback when value changes |
| `disabled` | `boolean` | `false` | Disables the select |
| `multiple` | `boolean` | `false` | Allow multiple selections |
| `items` | `Record<string, ReactNode>` \| `Array<{ value, label }>` | - | Value → label mapping for `SelectValue` to render in the trigger |

### SelectTrigger

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `size` | `"default" \| "sm"` | `"default"` | Size variant |
| `className` | `string` | - | Additional CSS classes |

### SelectItem

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `string` | - | Item value (required) |
| `disabled` | `boolean` | `false` | Disables the item |
