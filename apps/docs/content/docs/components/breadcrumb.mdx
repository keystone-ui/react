---
title: Breadcrumb
description: Navigation breadcrumb trail showing the user's current location within a hierarchy.
---

## Import

```tsx
import {
  Breadcrumb,
  BreadcrumbList,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbSeparator,
  BreadcrumbPage,
  BreadcrumbEllipsis,
} from "@keystoneui/react/breadcrumb";
```

## Usage

<ComponentPreview name="breadcrumb-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/breadcrumb.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="breadcrumb-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Composition

```text
Breadcrumb
└── BreadcrumbList
    ├── BreadcrumbItem
    │   ├── BreadcrumbLink
    │   └── BreadcrumbPage
    ├── BreadcrumbSeparator
    └── BreadcrumbEllipsis
```

## Examples

### Custom Separator

Use a custom separator character or icon:

<ComponentPreview name="breadcrumb-custom-separator" />


### Collapsed

Collapse middle items with an ellipsis when the trail is long:

<ComponentPreview name="breadcrumb-collapsed" />


### With Dropdown

Expand collapsed items into a dropdown menu:

<ComponentPreview name="breadcrumb-with-dropdown" />


### Link Component

Using a custom link component for routing integration:

<ComponentPreview name="breadcrumb-link-component" />

## RTL

RTL support in Keystone UI is partial: Base UI handles keyboard navigation and popup positioning when you supply a `DirectionProvider`, but the library's own spacing is written with physical utilities and does not mirror. See [the RTL guide](/docs/rtl) for what works today.

## API Reference

### BreadcrumbLink

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `href` | `string` | - | The URL the breadcrumb link points to |
| `className` | `string` | - | Additional CSS classes |
