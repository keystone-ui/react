---
title: Avatar
description: User avatar displaying a profile image with a text or icon fallback.
---

## Import

```tsx
import { Avatar, AvatarImage, AvatarFallback, AvatarGroup } from "@keystoneui/react/avatar";
```

## Usage

<ComponentPreview name="avatar-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/avatar.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="avatar-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Composition

```text
Avatar
├── AvatarImage
├── AvatarFallback
└── AvatarBadge

AvatarGroup
├── Avatar
└── AvatarGroupCount
```

## Examples

### Fallback

Text initials or icon shown when the image fails to load:

<ComponentPreview name="avatar-fallback" />


### Group

Stack multiple avatars together with an overlap effect:

<ComponentPreview name="avatar-group" />


### Sizes

<ComponentPreview name="avatar-sizes" />


### With Badge

Attach a status badge to the avatar corner:

<ComponentPreview name="avatar-with-badge" />


### Badge with Icon

An avatar with a badge containing an icon:

<ComponentPreview name="avatar-badge-with-icon" />


### Group with Count

An avatar group displaying an overflow count:

<ComponentPreview name="avatar-group-with-count" />

## RTL

RTL support in Keystone UI is partial: Base UI handles keyboard navigation and popup positioning when you supply a `DirectionProvider`, but the library's own spacing is written with physical utilities and does not mirror. See [the RTL guide](/docs/rtl) for what works today.


{/* backlinks:start */}

## Related Blocks

- [Profile Dropdown 01](/docs/blocks/profile-dropdown-01) — A user profile dropdown with avatar, level progress, and navigation menu.
- [Tickets 01](/docs/blocks/tickets-01) — A sortable, filterable tickets table with inline cell editing, a new-ticket modal, and a floating bulk-edit action bar.

{/* backlinks:end */}

## API Reference

### Avatar

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `size` | `"default" \| "sm" \| "lg" \| "xl"` | `"default"` | Size of the avatar |
| `className` | `string` | - | Additional CSS classes |

### AvatarImage

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `src` | `string` | - | Image source URL |
| `alt` | `string` | - | Alt text for the image |
