---
title: TagGroup
description: Managed group of tags with add and remove capabilities for multi-value selection.
---

## Import

```tsx
import { TagGroup, TagGroupItem } from "@keystoneui/react/tag-group";
```

## Usage

<ComponentPreview name="tag-group-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/tag-group.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="tag-group-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Composition

```text
TagGroup
└── TagGroupItem
```

## Examples

### Single Selection

Allow only one tag to be selected at a time:

<ComponentPreview name="tag-group-single-selection" />


### With Icons

Display icons alongside tag labels:

<ComponentPreview name="tag-group-with-icons" />


### Removable

Allow tags to be removed from the group:

<ComponentPreview name="tag-group-removable" />


### Controlled

Control the selected tags externally with state:

<ComponentPreview name="tag-group-controlled" />


### With Avatars

Tag group with avatar icons:

<ComponentPreview name="tag-group-with-avatars" />


### Disabled

Disabled tag group:

<ComponentPreview name="tag-group-disabled" />

## RTL

This component supports RTL layouts via the standard `dir="rtl"` attribute on a parent element. See [the RTL guide](/docs/rtl).

## API Reference

### TagGroup

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `selectionMode` | `"single" \| "multiple"` | `"multiple"` | Selection behavior |
| `value` | `string[]` | - | Controlled selected values |
| `onValueChange` | `(value: string[]) => void` | - | Callback when selection changes |
| `className` | `string` | - | Additional CSS classes |
