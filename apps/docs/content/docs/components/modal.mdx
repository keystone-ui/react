---
title: Modal
description: A dialog overlay for focused interactions like confirmations, forms, and detail views.
---

## Import

```tsx
import {
  Modal,
  ModalTrigger,
  ModalContent,
  ModalHeader,
  ModalTitle,
  ModalDescription,
  ModalBody,
  ModalFooter,
  ModalClose,
} from "@keystoneui/react/modal";
```

## Usage

<ComponentPreview name="modal-default" />

## Sizes

The Modal supports multiple width presets:

<ComponentPreview name="modal-sizes" />

| Size | Width | Use Case |
| --- | --- | --- |
| `sm` | 400px | Simple confirmations |
| `default` | 500px | Standard dialogs |
| `lg` | 640px | Forms and detail views |
| `xl` | 780px | Complex content |
| `full` | Full screen | Immersive experiences |

## Scrollable

A modal with scrollable body content for long forms:

<ComponentPreview name="modal-scrollable" />

## Sign Up Form

A practical sign-up form modal pattern:

<ComponentPreview name="modal-sign-up-form" />

## Controlled

Control the modal open state programmatically:

<ComponentPreview name="modal-controlled" />

## Nested

Stack multiple modals on top of each other:

<ComponentPreview name="modal-nested" />

## Sticky Footer

Modal with a sticky footer:

<ComponentPreview name="modal-sticky-footer" />

## Confirmation

Confirmation dialog modal:

<ComponentPreview name="modal-confirmation" />

## Multi Step

Multi-step wizard modal:

<ComponentPreview name="modal-multi-step" />

## Long Content

Modal with long scrollable content:

<ComponentPreview name="modal-long-content" />

## API Reference

### Modal

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `open` | `boolean` | - | Controlled open state |
| `onOpenChange` | `(open: boolean) => void` | - | Callback when open state changes |
| `defaultOpen` | `boolean` | `false` | Initial open state (uncontrolled) |

### ModalContent

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `size` | `"sm" \| "default" \| "lg" \| "xl" \| "full"` | `"default"` | Modal width |
| `className` | `string` | - | Additional CSS classes |
