---
title: Popover
description: Floating content panel anchored to a trigger element for contextual information or controls.
---

## Import

```tsx
import {
  Popover,
  PopoverTrigger,
  PopoverContent,
  PopoverHeader,
  PopoverTitle,
} from "@keystoneui/react/popover";
```

## Usage

<ComponentPreview name="popover-default" />

## With Form

Embed a form inside a popover for inline editing:

<ComponentPreview name="popover-with-form" />

## Alignments

Control the popover placement relative to the trigger:

<ComponentPreview name="popover-alignments" />

## With Arrow

Add an arrow indicator pointing to the trigger:

<ComponentPreview name="popover-with-arrow" />

## Open on Hover

Popover that opens on hover:

<ComponentPreview name="popover-open-on-hover" />

## API Reference

### PopoverContent

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `side` | `"top" \| "right" \| "bottom" \| "left"` | `"bottom"` | Preferred placement side |
| `align` | `"start" \| "center" \| "end"` | `"center"` | Alignment along the side axis |
| `sideOffset` | `number` | `4` | Offset from the trigger in pixels |
| `className` | `string` | - | Additional CSS classes |
