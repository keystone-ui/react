---
title: Accordion
description: Vertically stacked expandable and collapsible content sections for organizing dense information.
---

## Import

```tsx
import {
  Accordion,
  AccordionItem,
  AccordionHeader,
  AccordionTrigger,
  AccordionPanel,
} from "@keystoneui/react/accordion";
```

## Usage

<ComponentPreview name="accordion-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/accordion.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="accordion-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Composition

```text
Accordion
└── AccordionItem
    ├── AccordionHeader
    │   └── AccordionTrigger
    └── AccordionPanel
```

## Examples

### Variants

Different visual styles for the accordion:

<ComponentPreview name="accordion-variants" />


### Multiple

Allow multiple sections to be open simultaneously:

<ComponentPreview name="accordion-multiple" />


### Disabled

Disable individual accordion items to prevent expansion:

<ComponentPreview name="accordion-disabled" />


### Custom Chevron

Replace the default chevron with a custom icon:

<ComponentPreview name="accordion-custom-chevron" />

## RTL

This component supports RTL layouts via the standard `dir="rtl"` attribute on a parent element. See [the RTL guide](/docs/rtl).

## API Reference

### Accordion

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `type` | `"single" \| "multiple"` | `"single"` | Whether one or multiple items can be open |
| `defaultValue` | `string \| string[]` | - | Default open item(s) |
| `collapsible` | `boolean` | `false` | Allow all items to be collapsed (single mode) |

### AccordionItem

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `string` | - | Unique identifier for the item |
| `disabled` | `boolean` | `false` | Whether the item is disabled |
