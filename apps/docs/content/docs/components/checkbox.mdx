---
title: Checkbox
description: A toggle control for binary choices, supporting checked, unchecked, and indeterminate states.
---

## Import

```tsx
import { Checkbox } from "@keystoneui/react/checkbox";
```

## Usage

<ComponentPreview name="checkbox-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/checkbox.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="checkbox-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Examples

### With Description

Use Field components for labels and helper text:

<ComponentPreview name="checkbox-with-description" />


### Group

Multiple checkboxes in a fieldset:

<ComponentPreview name="checkbox-group" />


### Disabled

<ComponentPreview name="checkbox-disabled" />


### Pill Group

Pill-style checkboxes with animated check indicator that slides in from the left:

<ComponentPreview name="checkbox-pill-group" />


### Controlled

Controlled checkbox with state management:

<ComponentPreview name="checkbox-controlled" />

## RTL

RTL support in Keystone UI is partial: Base UI handles keyboard navigation and popup positioning when you supply a `DirectionProvider`, but the library's own spacing is written with physical utilities and does not mirror. See [the RTL guide](/docs/rtl) for what works today.


{/* backlinks:start */}

## Related Blocks

- [Tickets 01](/docs/blocks/tickets-01) — A sortable, filterable tickets table with inline cell editing, a new-ticket modal, and a floating bulk-edit action bar.

{/* backlinks:end */}

## API Reference

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `checked` | `boolean` | - | Controlled checked state |
| `defaultChecked` | `boolean` | `false` | Default checked state (uncontrolled) |
| `onCheckedChange` | `(checked: boolean) => void` | - | Callback when checked state changes |
| `disabled` | `boolean` | `false` | Disables the checkbox |
| `indeterminate` | `boolean` | `false` | Shows indeterminate state |
| `className` | `string` | - | Additional CSS classes |
