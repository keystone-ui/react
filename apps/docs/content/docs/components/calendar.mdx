---
title: Calendar
description: Interactive date calendar for browsing and selecting dates by month.
---

## Import

```tsx
import { Calendar } from "@keystoneui/react/calendar";
```

## Usage

<ComponentPreview name="calendar-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/calendar.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="calendar-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Examples

### Range

Select a date range with start and end dates:

<ComponentPreview name="calendar-range" />


### Month & Year Navigation

Navigate by month and year with dropdowns:

<ComponentPreview name="calendar-month-year" />


### Presets

Quick selection with predefined date presets:

<ComponentPreview name="calendar-presets" />


### Booked Dates

Display unavailable or booked dates:

<ComponentPreview name="calendar-booked-dates" />


### Date Time

Calendar with time selection:

<ComponentPreview name="calendar-date-time" />


### Week Numbers

Calendar displaying week numbers:

<ComponentPreview name="calendar-week-numbers" />

## RTL

This component supports RTL layouts via the standard `dir="rtl"` attribute on a parent element. See [the RTL guide](/docs/rtl).

## API Reference

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `selected` | `Date \| DateRange` | - | Currently selected date(s) |
| `onSelect` | `(date: Date \| DateRange) => void` | - | Callback when date is selected |
| `mode` | `"single" \| "range" \| "multiple"` | `"single"` | Selection mode |
| `disabled` | `Date[] \| ((date: Date) => boolean)` | - | Dates to disable |
| `className` | `string` | - | Additional CSS classes |
