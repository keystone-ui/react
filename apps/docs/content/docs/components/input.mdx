---
title: Input
description: A form input field with consistent styling and focus states.
---

## Import

```tsx
import { Input } from "@keystoneui/react/input";
```

## Usage

<ComponentPreview name="input-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/input.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="input-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Examples

### With Field

Use `Field`, `FieldLabel`, and `FieldDescription` for accessible, structured form inputs:

<ComponentPreview name="input-with-field" />


### Disabled

Use the `disabled` prop on the input and `data-disabled` on the `Field` to style the disabled state:

<ComponentPreview name="input-disabled" />


### With Error

Use `aria-invalid` on the input and `data-invalid` on the `Field` to show error styling. Use `FieldError` for the error message:

<ComponentPreview name="input-with-error" />


### File Input

<ComponentPreview name="input-file" />


### Form Example

A complete form built with `FieldGroup`, `Field`, and `Input`:

<ComponentPreview name="input-form-example" />


### With Label

Input with a label and description:

<ComponentPreview name="input-with-label" />


### Sizes

Different input sizes:

<ComponentPreview name="input-sizes" />


### With Icon

Input with an icon:

<ComponentPreview name="input-with-icon" />


### With Button

Input combined with a button:

<ComponentPreview name="input-with-button" />


### With Password Toggle

Password input with show/hide toggle:

<ComponentPreview name="input-with-password-toggle" />


### With Helper Text

Input with helper text:

<ComponentPreview name="input-with-helper-text" />


### With Prefix

Input with a text prefix:

<ComponentPreview name="input-with-prefix" />


### Types

Different input types:

<ComponentPreview name="input-types" />

## RTL

RTL support in Keystone UI is partial: Base UI handles keyboard navigation and popup positioning when you supply a `DirectionProvider`, but the library's own spacing is written with physical utilities and does not mirror. See [the RTL guide](/docs/rtl) for what works today.


{/* backlinks:start */}

## Related Blocks

- [Sign in 01](/docs/blocks/signin-01) — A simple sign-in form inside a card with email and password fields.
- [Sign in 02](/docs/blocks/signin-02) — A two-column sign-in page with a cover image and GitHub social login.
- [Sign in 03](/docs/blocks/signin-03) — A sign-in page with a form and image side by side inside a card.
- [Sign in 04](/docs/blocks/signin-04) — A minimal email-only sign-in page with Apple and Google social providers.
- [Signup 01](/docs/blocks/signup-01) — A simple signup form inside a card with name, email, and password fields.
- [Signup 02](/docs/blocks/signup-02) — A two-column signup page with a cover image and GitHub social login.
- [Signup 03](/docs/blocks/signup-03) — A signup page with a card form and side-by-side password fields.
- [Signup 04](/docs/blocks/signup-04) — A signup page with a form and image side by side inside a card.
- [Signup 05](/docs/blocks/signup-05) — A minimal email-only signup page with Apple and Google social providers.
- [Tickets 01](/docs/blocks/tickets-01) — A sortable, filterable tickets table with inline cell editing, a new-ticket modal, and a floating bulk-edit action bar.

{/* backlinks:end */}

## API Reference

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `type` | `string` | `"text"` | HTML input type |
| `size` | `"default" \| "sm"` | `"default"` | Size variant of the input |
| `placeholder` | `string` | - | Placeholder text |
| `disabled` | `boolean` | `false` | Disables the input |
| `aria-invalid` | `boolean` | `false` | Shows error styling |
| `htmlSize` | `number` | - | Native HTML `size` attribute |
| `className` | `string` | - | Additional CSS classes |

The Input forwards all standard HTML `<input>` props.
