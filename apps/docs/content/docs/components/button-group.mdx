---
title: ButtonGroup
description: Groups related buttons together with consistent spacing and connected styling.
---

## Import

```tsx
import { ButtonGroup } from "@keystoneui/react/button-group";
```

## Usage

<ComponentPreview name="button-group-default" />

## Vertical

Stack buttons vertically:

<ComponentPreview name="button-group-vertical" />

## Sizes

Different size presets for the group:

<ComponentPreview name="button-group-sizes" />

## Toolbar

A toolbar pattern with grouped action buttons:

<ComponentPreview name="button-group-toolbar" />

## Split Button

A primary action with a dropdown for secondary options:

<ComponentPreview name="button-group-split-button" />

## With Input

Combine buttons with an input field:

<ComponentPreview name="button-group-with-input" />

## With Select

Combine a Select with an Input and action button for inline composite controls:

<ComponentPreview name="button-group-with-select" />

## With Dropdown Menu

Pair buttons with a DropdownMenu trigger for split-action patterns like GitHub-style repo actions:

<ComponentPreview name="button-group-with-dropdown-menu" />

## Email Toolbar

An email action toolbar using nested ButtonGroups with dropdown menus and sub-menus:

<ComponentPreview name="button-group-email-toolbar" />

## Accessibility

The ButtonGroup component has the `role` attribute set to `group`. Use `aria-label` or `aria-labelledby` to label the button group.

```tsx
<ButtonGroup aria-label="Button group">
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</ButtonGroup>
```

## ButtonGroup vs ToggleGroup

Use the **ButtonGroup** component when you want to group buttons that perform an action.
Use the **ToggleGroup** component when you want to group buttons that toggle a state.

## API Reference

### ButtonGroup

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | Layout direction |
| `size` | `"default" \| "sm" \| "lg"` | `"default"` | Size of buttons in the group |
| `className` | `string` | - | Additional CSS classes |
