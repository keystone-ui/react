---
title: Alert
description: Prominent message banner for displaying important information, warnings, or errors to users.
---

## Import

```tsx
import { Alert, AlertTitle, AlertDescription, AlertAction } from "@keystoneui/react/alert";
```

## Usage

<ComponentPreview name="alert-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/alert.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="alert-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Composition

```text
Alert
├── Icon
├── AlertTitle
├── AlertDescription
└── AlertAction
```

## Examples

### Variants

The Alert supports semantic color variants for different message types:

<ComponentPreview name="alert-variants" />

| Variant | Use Case |
| --- | --- |
| `default` | General informational messages |
| `success` | Successful operations or confirmations |
| `warning` | Caution or attention needed |
| `error` | Errors or critical failures |
| `info` | Informational tips or notes |


### With Action

Include an action button inside the alert:

<ComponentPreview name="alert-with-action" />


### Without Icon

Alerts without a leading icon:

<ComponentPreview name="alert-without-icon" />


### Custom Colors

Apply custom color styling for brand-specific alerts:

<ComponentPreview name="alert-custom-colors" />

## RTL

This component supports RTL layouts via the standard `dir="rtl"` attribute on a parent element. See [the RTL guide](/docs/rtl).

## API Reference

### Alert

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `"default" \| "success" \| "warning" \| "error" \| "info"` | `"default"` | Semantic color variant |
| `className` | `string` | - | Additional CSS classes |
