---
title: Slider
description: Range slider input for selecting a numeric value within a defined range.
---

## Import

```tsx
import {
  Slider,
  SliderControl,
  SliderTrack,
  SliderIndicator,
  SliderThumb,
  SliderValue,
} from "@keystoneui/react/slider";
```

## Usage

<ComponentPreview name="slider-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/slider.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="slider-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Composition

```text
Slider
└── SliderControl
    └── SliderTrack
        ├── SliderIndicator
        ├── SliderThumb
        └── SliderValue
```

## Examples

### With Label

Display the current value alongside the slider:

<ComponentPreview name="slider-with-label" />


### Range

A dual-thumb slider for selecting a range of values:

<ComponentPreview name="slider-range" />


### Disabled

<ComponentPreview name="slider-disabled" />


### Controlled

Controlled slider with value display:

<ComponentPreview name="slider-controlled" />


### Custom Step

Slider with custom step values:

<ComponentPreview name="slider-custom-step" />

## RTL

This component supports RTL layouts via the standard `dir="rtl"` attribute on a parent element. See [the RTL guide](/docs/rtl).


{/* backlinks:start */}

## Related Blocks

- [Betting Panel 02](/docs/blocks/betting-panel-02) — A slider-based betting panel with a gradient risk slider for visual feedback on the chosen risk level.

{/* backlinks:end */}

## API Reference

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `defaultValue` | `number \| number[]` | - | Default value(s) |
| `value` | `number \| number[]` | - | Controlled value(s) |
| `onValueChange` | `(value: number[]) => void` | - | Callback when value changes |
| `min` | `number` | `0` | Minimum value |
| `max` | `number` | `100` | Maximum value |
| `step` | `number` | `1` | Step increment |
| `disabled` | `boolean` | `false` | Whether the slider is disabled |
