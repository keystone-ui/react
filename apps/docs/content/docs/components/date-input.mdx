---
title: DateInput
description: Date picker input field for selecting dates with keyboard and calendar support.
---

## Import

```tsx
import { DateInput } from "@keystoneui/react/date-input";
```

## Usage

<ComponentPreview name="date-input-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/date-input.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="date-input-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Examples

### Range

Select a date range with start and end values:

<ComponentPreview name="date-input-range" />


### With Input

Combine a date picker with a text input for manual entry:

<ComponentPreview name="date-input-with-input" />


### Presets

Quick date selection with predefined options like "Today" or "Last 7 days":

<ComponentPreview name="date-input-presets" />


### Date of Birth

A date picker configured for selecting a date of birth with month/year navigation:

<ComponentPreview name="date-input-date-of-birth" />


### With Time

Date picker with time selection:

<ComponentPreview name="date-input-with-time" />


### Natural Language

Date input with natural language hints:

<ComponentPreview name="date-input-natural-language" />


### Controlled

Controlled date picker:

<ComponentPreview name="date-input-controlled" />

## RTL

RTL support in Keystone UI is partial: Base UI handles keyboard navigation and popup positioning when you supply a `DirectionProvider`, but the library's own spacing is written with physical utilities and does not mirror. See [the RTL guide](/docs/rtl) for what works today.

## API Reference

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `placeholder` | `string` | - | Placeholder text for the input |
| `className` | `string` | - | Additional CSS classes |
