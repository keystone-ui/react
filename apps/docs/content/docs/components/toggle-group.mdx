---
title: ToggleGroup
description: Group of toggle buttons where one or multiple options can be selected.
---

## Import

```tsx
import { ToggleGroup, ToggleGroupItem } from "@keystoneui/react/toggle-group";
```

## Usage

<ComponentPreview name="toggle-group-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/toggle-group.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="toggle-group-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Composition

```text
ToggleGroup
└── ToggleGroupItem
```

## ButtonGroup vs ToggleGroup

Use the **ButtonGroup** component when you want to group buttons that perform an action.
Use the **ToggleGroup** component when you want to group buttons that toggle a state.

## Heights in a toolbar

`ToggleGroup` shares [`Button`](/docs/components/button)'s height ladder, so a toolbar mixing them lines up with no size props at all:

| | `xs` | `sm` | `default` | `lg` |
| --- | --- | --- | --- | --- |
| `Button` | 24px | 32px | **40px** | 48px |
| `Toggle` / `ToggleGroup` | — | 32px | **40px** | 48px |

```tsx
{/* both 40px, nothing to remember */}
<ToggleGroup>…</ToggleGroup>
<Button>Export</Button>
```

The rule that matters is the general one: **pick one tier for the whole row.** Mixing tiers between neighbours — a `size="sm"` button beside a default-height input — is a gap of 8px, and it is easy to miss on a dark background and obvious once seen.

`ToggleGroup` contributes no height of its own; the group is exactly as tall as its items. So setting `size` on the group, or on each item, gives the same result, and there is no wrapper padding to account for.

<Callout type="info">
  Toggle used to default to 36px, which is the popup item height. That was
  inherited from shadcn when the component was reworked and never revisited —
  no toggle in this library is ever rendered inside a menu. 36px remains the
  popup tier, reachable through `DropdownMenuContent`'s own `size` prop, and is
  deliberately not a control tier.
</Callout>

## Examples

### Outline

Outline variant for the toggle group:

<ComponentPreview name="toggle-group-outline" />


### Secondary

Filled background when unpressed (uses `bg-secondary`) and inverts to `bg-foreground` when pressed. Best for segmented-control patterns where unpressed items should still read as part of a control rather than empty space.

<ComponentPreview name="toggle-group-secondary" />


### Single Selection

Allow only one item to be selected at a time:

<ComponentPreview name="toggle-group-single" />


### Sizes

<ComponentPreview name="toggle-group-sizes" />


### Spacing

Use the `spacing` prop to control the gap between items. When `0` (default), items join together with connected borders. Values greater than `0` add a gap using the spacing scale.

<ComponentPreview name="toggle-group-spacing" />


### Text Labels

Toggle groups work well with text labels for filter-style and segmented controls.

<ComponentPreview name="toggle-group-text-labels" />


### Vertical

Use `orientation="vertical"` for vertical toggle groups.

<ComponentPreview name="toggle-group-vertical" />


### Custom

Custom-styled toggle items with `spacing`, `className` overrides, and integration with the Field component for form context.

<ComponentPreview name="toggle-group-custom" />


### Disabled

Disabled toggle group:

<ComponentPreview name="toggle-group-disabled" />

## RTL

RTL support in Keystone UI is partial: Base UI handles keyboard navigation and popup positioning when you supply a `DirectionProvider`, but the library's own spacing is written with physical utilities and does not mirror. See [the RTL guide](/docs/rtl) for what works today.


{/* backlinks:start */}

## Related Blocks

- [Betting Panel 01](/docs/blocks/betting-panel-01) — A betting panel with inline ½ / 2× multipliers inside the bet amount field and risk-level toggles.
- [Betting Panel 03](/docs/blocks/betting-panel-03) — A betting panel with quick-pick preset amounts for rapid bet placement.
- [Betting Panel 04](/docs/blocks/betting-panel-04) — A stepper-based betting panel with circular +/− controls for bet amount and rows.
- [Dashboard 01](/docs/blocks/dashboard-01) — An analytics dashboard — KPI row with semantic deltas, a stacked revenue chart with a toggleable legend, a sessions area chart, and a sortable, paginated top-pages table.
- [Tickets 01](/docs/blocks/tickets-01) — A sortable, filterable tickets table with inline cell editing, a new-ticket modal, and a floating bulk-edit action bar.

{/* backlinks:end */}

## API Reference

### ToggleGroup

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `"default" \| "outline" \| "secondary"` | `"default"` | Visual style variant |
| `size` | `"default" \| "sm" \| "lg"` | `"default"` | Size of the toggle items |
| `spacing` | `number` | `0` | Gap between items (0 = joined with connected borders) |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | Layout direction |
| `multiple` | `boolean` | `false` | Allow multiple items to be selected |
| `value` | `string[]` | - | Controlled selected value(s) |
| `onValueChange` | `(value: string[]) => void` | - | Callback when value changes |
| `disabled` | `boolean` | `false` | Whether the entire group is disabled |

### ToggleGroupItem

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `string` | - | Unique value for the item |
| `disabled` | `boolean` | `false` | Whether the item is disabled |
