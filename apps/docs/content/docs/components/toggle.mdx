---
title: Toggle
description: Two-state toggle button that switches between pressed and unpressed states.
---

## Import

```tsx
import { Toggle } from "@keystoneui/react/toggle";
```

## Usage

<ComponentPreview name="toggle-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/toggle.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="toggle-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Examples

### Outline

A bordered outline variant:

<ComponentPreview name="toggle-outline" />


### Secondary

Filled when unpressed (uses `bg-secondary`) and inverted to `bg-foreground` when pressed. Useful when an unpressed toggle would otherwise read as empty space — e.g., a 3-pill segmented control where only one is selected.

<ComponentPreview name="toggle-secondary" />


### With Text

Toggle with a text label:

<ComponentPreview name="toggle-with-text" />


### Sizes

<ComponentPreview name="toggle-sizes" />


### Disabled

Disabled toggle button:

<ComponentPreview name="toggle-disabled" />


### Pressed

Toggle in pressed state:

<ComponentPreview name="toggle-pressed" />

## RTL

RTL support in Keystone UI is partial: Base UI handles keyboard navigation and popup positioning when you supply a `DirectionProvider`, but the library's own spacing is written with physical utilities and does not mirror. See [the RTL guide](/docs/rtl) for what works today.

## API Reference

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `"default" \| "outline" \| "secondary"` | `"default"` | Visual style variant |
| `size` | `"default" \| "sm" \| "lg"` | `"default"` | Size of the toggle |
| `pressed` | `boolean` | - | Controlled pressed state |
| `onPressedChange` | `(pressed: boolean) => void` | - | Callback when pressed state changes |
| `disabled` | `boolean` | `false` | Whether the toggle is disabled |
