---
title: DescriptionList
description: Displays term-description pairs for presenting metadata, settings, or key-value information.
---

## Import

```tsx
import {
  DescriptionList,
  DescriptionListItem,
  DescriptionListTerm,
  DescriptionListDetails,
} from "@keystoneui/react/description-list";
```

## Usage

<ComponentPreview name="description-list-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/description-list.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="description-list-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Composition

```text
DescriptionList
└── DescriptionListItem
    ├── DescriptionListTerm
    └── DescriptionListDetails
```

## Examples

### Card Variant

A variant with rounded corners and background fill on each row:

<ComponentPreview name="description-list-card-variant" />


### Compact

A smaller, denser layout for tighter spaces:

<ComponentPreview name="description-list-compact" />


### In Card

Embed a description list inside a Card for structured content blocks:

<ComponentPreview name="description-list-in-card" />


### Custom Values

Description list with custom value rendering:

<ComponentPreview name="description-list-custom-values" />


### Order Summary

Order summary using description list:

<ComponentPreview name="description-list-order-summary" />

## RTL

RTL support in Keystone UI is partial: Base UI handles keyboard navigation and popup positioning when you supply a `DirectionProvider`, but the library's own spacing is written with physical utilities and does not mirror. See [the RTL guide](/docs/rtl) for what works today.

## API Reference

### DescriptionList

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `size` | `"default" \| "sm"` | `"default"` | The size variant |
| `variant` | `"default" \| "card"` | `"default"` | The visual variant |
| `className` | `string` | - | Additional CSS classes |
