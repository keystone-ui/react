---
title: Switch
description: A toggle control for boolean states with smooth animation.
---

## Import

```tsx
import { Switch } from "@keystoneui/react/switch";
```

## Usage

<ComponentPreview name="switch-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/switch.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="switch-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Examples

### With Description

Pair with Field components for label and helper text:

<ComponentPreview name="switch-with-description" />


### Sizes

<ComponentPreview name="switch-sizes" />


### Disabled

<ComponentPreview name="switch-disabled" />


### Choice Card

Switch in a choice card layout:

<ComponentPreview name="switch-choice-card" />

## RTL

RTL support in Keystone UI is partial: Base UI handles keyboard navigation and popup positioning when you supply a `DirectionProvider`, but the library's own spacing is written with physical utilities and does not mirror. See [the RTL guide](/docs/rtl) for what works today.

## API Reference

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `checked` | `boolean` | - | Controlled checked state |
| `defaultChecked` | `boolean` | `false` | Default state (uncontrolled) |
| `onCheckedChange` | `(checked: boolean) => void` | - | Callback when state changes |
| `disabled` | `boolean` | `false` | Disables the switch |
| `size` | `"default" \| "sm"` | `"default"` | Size variant |
| `className` | `string` | - | Additional CSS classes |
