---
title: Stepper
description: Multi-step progress indicator for guiding users through sequential workflows.
---

## Import

```tsx
import { Stepper, StepperStep, StepperContent, useStepper } from "@keystoneui/react/stepper";
```

## Usage

<ComponentPreview name="stepper-default" />

## Installation

<CodeBlockTabs items={["CLI", "Manual"]}>

```bash tab="CLI"
npx shadcn@latest add https://keystoneui.io/r/stepper.json
```

<div tab="Manual">

<Steps>

<Step>Install dependencies.</Step>

```bash
pnpm add @keystoneui/react @base-ui/react
```

<Step>Copy the source.</Step>

<ComponentSource name="stepper-default" />

<Step>Update import paths to match your project.</Step>

</Steps>

</div>

</CodeBlockTabs>

## Composition

```text
Stepper
└── StepperContent
    └── StepperStep
```

## Examples

### With Indicator

Show step numbers or check marks for completed steps:

<ComponentPreview name="stepper-with-indicator" />


### Controlled

Manage the active step externally with state:

<ComponentPreview name="stepper-controlled" />

## RTL

RTL support in Keystone UI is partial: Base UI handles keyboard navigation and popup positioning when you supply a `DirectionProvider`, but the library's own spacing is written with physical utilities and does not mirror. See [the RTL guide](/docs/rtl) for what works today.


{/* backlinks:start */}

## Related Blocks

- [Tickets 01](/docs/blocks/tickets-01) — A sortable, filterable tickets table with inline cell editing, a new-ticket modal, and a floating bulk-edit action bar.

{/* backlinks:end */}

## API Reference

### Stepper

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `activeStep` | `number` | `0` | The currently active step index |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | Layout direction |

### StepperStep

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | - | Additional CSS classes |
